# frozen_string_literal: true

RSpec.describe Indirect::Trusted::Publishing do
  it "has a version number" do
    expect(Indirect::Trusted::Publishing::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
